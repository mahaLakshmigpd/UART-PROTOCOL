# UART-PROTOCOL
UART (Universal Asynchronous Receiver-Transmitter) is a 2-wire (TX/RX), asynchronous, full-duplex protocol used for serial communication between devices (e.g., MCU to sensors). It uses no shared clock, relying on matching baud rates and data frames (start/stop bits) for timing.
